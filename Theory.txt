ASSUMPTIONS IN LINEAR REGRESSION:
1) Linearity- y predicted and ploted to check the linearity
2) Homoscopseedasticity- (Constant Varianceof Error) Breusch-Page test and White test is used
3) Normality of Error- Anderson Darling test is used
4) Independance on Error- Durbin Whatson test is used
5) No Multicollinearity- Variance Inflation Factor (VIF) used to check
6) No Influential Outliers- used Cook's Distance and 

Cost Function- Mean Square Error 
Hypothesis Function- assumptions relation between X an Y    { h0(x)=0o+01x }
J(o)= (1/2m) sum{1 to m} (y_true - y_predicted)^2

Visualise cost fuction and Gradient Desecent- The cost function of linear regression is convex (parabolic shape) Gradient-Descent starts at the random point and moves downhill to find the global minimum
Types: 
1) Batch-


x= 1, 2, 3 
Y= 2, 2.8, 3.6
initially 0o= 0 and 01=0 (m=3)

Technique of Feature Selection:
A) Manual Feature Selection
B) Statistical Method- Corelation anlysis P-value hypothesis testing (p-value > 0.05 for statistically insignificant).  
                        Remove low variance feature. Solved by ANOVA.
    Pearson Cprrelation ( r= sum{ (x1-x_mean)(y1-y_mean) }/{ (sum(x1-x_mean)^2 . sum(y1-y_mean)^2)^0.5 })
    df.corr(method="pearson") - numerical
    df.corr(method="spearman") - ordinal values
    df.corr(method="kendall") - ordinal values
    Variance Threshhold- sum(x1-x)^2 / N
C) Automated Feature Selection Methods- Recursive function Elimination(RFE), Lesso Regression (unimp feature=0), 
                                        Feature Importance (Tree Based Model)
    Lesso Regression- L1 Regularization when you want to remove irrelevant feature
    Ridge Regression- When you want all feature but it needed too be shrunk 


Accuracy= (TP+TN)/ (TP+TN+FP+FN)
Precison= TP/(TP+FP)
Recall(sensitivity)= TP/(TP+FN)
F1 Score= 2* (Precison*Recall)/(Precison+Recall)

-------------------------------------------------------------------------------------------------------------------------------

Logistic Reegression:

Types- Binomial, Multinomial, Ordinal
Assumptions- Independent observations, Binary dependent variables, No outliers, Large dataset